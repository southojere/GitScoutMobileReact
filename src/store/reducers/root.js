const initState = {
    city: "",
    sortBy: "Followers",
    numberResults: 20
}

const reducer = (state = initState, action) => {
    switch (action.type) {
        default:
            return initState;
    }
}

export default reducer;